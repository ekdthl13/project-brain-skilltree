# [ORDER #001] Bootstrap Project Scaffold

## Role
- Coding/Developer Agent (e.g., Codex, Claude)

## Objective
- Initialize basic directory structure and run standard test framework sanity check.

## Tasks
1. Verify package.json contains all required dependencies.
2. Initialize directory structure: `src/`, `tests/`, `docs/`.
3. Write a simple dummy test and ensure `npm run test` passes.

## Definition of Done
- Workspace directory matches design layout.
- Test suites run and pass without failures.
- No absolute user paths or secrets are hardcoded.
